name: Blog + Scout + AdSense Code Quality

on:
  push:
    branches:
      - feat/blog-monetization-os
      - feat/scout-de-cria
      - feat/scout-unity-client
    paths:
      - "index.html"
      - "src/**"
      - "public/**"
      - "unity/**"
      - "supabase/migrations/**"
      - "scripts/adsense-*.mjs"
      - "scripts/unity-scout-audit.mjs"
      - ".github/workflows/adsense-*.yml"
      - "package*.json"
  pull_request:
    paths:
      - "index.html"
      - "src/**"
      - "public/**"
      - "unity/**"
      - "supabase/migrations/**"
      - "scripts/adsense-*.mjs"
      - "scripts/unity-scout-audit.mjs"
      - ".github/workflows/adsense-*.yml"
      - "package*.json"

permissions:
  contents: read

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install
        run: npm install --legacy-peer-deps --no-audit --no-fund

      - name: AdSense readiness audit
        run: npm run ads:audit

      - name: Scout Unity static audit
        run: node scripts/unity-scout-audit.mjs

      - name: Build web portal
        run: npm run build

      - name: Test web + Scout engines
        run: npm test
